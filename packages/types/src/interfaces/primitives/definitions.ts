export default {
    types: {
        CurrencyId: {
            _enum: ['ACA', 'AUSD', 'DOT', 'XBTC', 'LDOT']
        },
        AirDropCurrencyId: {
            _enum: ['KAR', 'ACA']
        }
    }
}
